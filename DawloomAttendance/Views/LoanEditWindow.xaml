<Window x:Class="DawloomAttendance.Views.LoanEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Loan" Height="380" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Date" VerticalAlignment="Center" Margin="0,0,0,8" />
        <DatePicker Grid.Row="0" Grid.Column="1" x:Name="DatePick" Margin="0,0,0,8" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Type" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="TypeBox" IsEditable="True" Margin="0,0,0,8">
            <ComboBoxItem>Advance</ComboBoxItem>
            <ComboBoxItem>Personal</ComboBoxItem>
            <ComboBoxItem>Emergency</ComboBoxItem>
            <ComboBoxItem>Equipment</ComboBoxItem>
            <ComboBoxItem>Medical</ComboBoxItem>
            <ComboBoxItem>Other</ComboBoxItem>
        </ComboBox>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Payment (amount)" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PaymentBox" Margin="0,0,0,8" />

        <CheckBox Grid.Row="3" Grid.Column="1" x:Name="InstallmentCheck" Content="Deduct in monthly installments"
                  VerticalAlignment="Center" Margin="0,0,0,8"
                  Checked="InstallmentCheck_Changed" Unchecked="InstallmentCheck_Changed" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Installment / month" VerticalAlignment="Center" Margin="0,0,0,2" />
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="InstallmentBox" Margin="0,0,0,2" IsEnabled="False" />
        <TextBlock Grid.Row="5" Grid.Column="1" x:Name="InstallmentHint"
                   Text="Unchecked: the whole amount is deducted on the next salary slip."
                   Foreground="Gray" FontSize="10.5" Margin="0,0,0,8" TextWrapping="Wrap" />

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Remarks" VerticalAlignment="Top" Margin="0,4,0,0" />
        <TextBox Grid.Row="6" Grid.Column="1" x:Name="RemarksBox" Margin="0,4,0,0"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="50" />

        <StackPanel Grid.Row="7" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Add" Width="80" Height="28" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click" />
            <Button Content="Cancel" Width="80" Height="28" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
