<UserControl x:Class="DawloomAttendance.Views.LoansWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Loans" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0" />
            <Button x:Name="AddButton" Content="Add Loan" Width="100" Height="30" Margin="0,0,8,0" Click="AddButton_Click" />
            <Button x:Name="DeleteButton" Content="Delete Loan" Width="100" Height="30" Margin="0,0,8,0" Click="DeleteLoanButton_Click" />
            <Button x:Name="RefreshButton" Content="Refresh" Width="80" Height="30" Click="RefreshButton_Click" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1.4*" />
            </Grid.ColumnDefinitions>

            <!-- Master: one row per employee with loan totals -->
            <DataGrid Grid.Column="0" x:Name="MasterGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" SelectionMode="Single" Margin="0,0,8,0"
                      SelectionChanged="MasterGrid_SelectionChanged"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Enroll" Binding="{Binding EnrollNumber}" Width="60" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Total loan" Binding="{Binding TotalLoan, StringFormat=0}" Width="80" />
                    <DataGridTextColumn Header="Outstanding" Binding="{Binding Outstanding, StringFormat=0}" Width="85" />
                    <DataGridTextColumn Header="#" Binding="{Binding LoanCount}" Width="35" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- Detail: the selected employee's individual loans -->
            <DockPanel Grid.Column="1">
                <TextBlock x:Name="DetailHeader" DockPanel.Dock="Top" Text="Select an employee to see their loans"
                           FontWeight="SemiBold" Margin="0,0,0,6" />
                <DataGrid x:Name="DetailGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          SelectionMode="Single" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=yyyy-MM-dd}" Width="90" />
                        <DataGridTextColumn Header="Payment" Binding="{Binding Amount, StringFormat=0}" Width="75" />
                        <DataGridTextColumn Header="Installment" Binding="{Binding InstallmentDisplay}" Width="80" />
                        <DataGridTextColumn Header="Deducted" Binding="{Binding Deducted, StringFormat=0}" Width="75" />
                        <DataGridTextColumn Header="Outstanding" Binding="{Binding Outstanding, StringFormat=0}" Width="85" />
                        <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>

        <TextBlock Grid.Row="2" x:Name="StatusText" Foreground="Gray" Margin="0,8,0,0" />
    </Grid>
</UserControl>
