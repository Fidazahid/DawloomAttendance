<UserControl x:Class="DawloomAttendance.Views.BackupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Backup folder:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="DirBox" Width="380" VerticalAlignment="Center" Margin="0,0,8,0" />
            <Button Content="Save folder" Width="90" Height="28" Margin="0,0,8,0" Click="SaveDir_Click" />
            <Button Content="Open folder" Width="90" Height="28" Click="OpenDir_Click" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BackupNowButton" Content="Backup Now" Width="110" Height="30" Margin="0,0,8,0" Click="BackupNow_Click" />
            <Button x:Name="RefreshButton" Content="Refresh" Width="80" Height="30" Margin="0,0,8,0" Click="Refresh_Click" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="Gray" />
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="Grid" IsReadOnly="True" AutoGenerateColumns="False"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Backup file" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Size" Binding="{Binding SizeText}" Width="100" />
                <DataGridTextColumn Header="Created" Binding="{Binding Created}" Width="160" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="RestoreButton" Content="Restore selected" Width="130" Height="30" Click="Restore_Click" />
            <TextBlock VerticalAlignment="Center" Foreground="Gray" Margin="12,0,0,0"
                       Text="A daily backup runs automatically on startup. Restore replaces the live database (app restarts)." />
        </StackPanel>
    </Grid>
</UserControl>
