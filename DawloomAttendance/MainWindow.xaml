<Window x:Class="DawloomAttendance.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DawloomAttendance"
        mc:Ignorable="d"
        Title="Dawloom Attendance" Height="640" Width="1040">
    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="16,0,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#FFDDDDDD" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FF3E3E42" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#FF2D2D30">
            <StackPanel Margin="0,14,0,0">
                <TextBlock Text="Dawloom" Foreground="White" FontSize="20" FontWeight="Bold" Margin="16,0,0,0" />
                <TextBlock Text="Attendance" Foreground="#FF9E9E9E" Margin="16,0,0,14" />

                <StackPanel Orientation="Horizontal" Margin="16,0,0,4">
                    <Ellipse x:Name="StatusDot" Width="12" Height="12" Fill="Gray" VerticalAlignment="Center" />
                    <TextBlock x:Name="StatusText" Text="Disconnected" Foreground="White" Margin="6,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock x:Name="StatusDetail" Foreground="#FFFF8A80" TextWrapping="Wrap" FontSize="11" Margin="16,0,12,14" />

                <Button Style="{StaticResource NavButton}" Content="Connection" Click="NavConnection_Click" />
                <Button Style="{StaticResource NavButton}" Content="Dashboard" Click="NavDashboard_Click" />
                <Button Style="{StaticResource NavButton}" Content="Employees" Click="NavEmployees_Click" />
                <Button Style="{StaticResource NavButton}" Content="Holidays" Click="NavHolidays_Click" />
                <Button Style="{StaticResource NavButton}" Content="Leaves" Click="NavLeaves_Click" />
                <Button Style="{StaticResource NavButton}" Content="Attendance" Click="NavAttendance_Click" />
                <Button Style="{StaticResource NavButton}" Content="Reports" Click="NavReports_Click" />
                <Button Style="{StaticResource NavButton}" Content="Backup" Click="NavBackup_Click" />
                <Button Style="{StaticResource NavButton}" Content="Audit" Click="NavAudit_Click" />
                <Button Style="{StaticResource NavButton}" Content="Settings" Click="NavSettings_Click" />
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Column="1">
            <!-- Home / Connection panel -->
            <Grid x:Name="HomePanel" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="ZKTeco K70 Connection" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Name="TargetText" VerticalAlignment="Center" Text="Target: (not configured)" />
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="SyncTimeButton" Content="Sync Time" Width="90" Height="28" Margin="0,0,8,0"
                                    Click="SyncTimeButton_Click" IsEnabled="False"
                                    ToolTip="Set the device clock to this PC's current time." />
                            <Button x:Name="SettingsButton" Content="Settings…" Width="90" Height="28" Click="SettingsButton_Click" />
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
                    <Button x:Name="ProbeButton" Content="TCP Probe" Width="110" Height="30" Click="ProbeButton_Click" Margin="0,0,8,0"
                            ToolTip="Check the device answers on IP:port (no SDK required)." />
                    <Button x:Name="ConnectButton" Content="Connect" Width="110" Height="30" Click="ConnectButton_Click" Margin="0,0,8,0"
                            ToolTip="Start a self-healing connection: connect, keep-alive ping, auto-reconnect." />
                    <Button x:Name="DisconnectButton" Content="Disconnect" Width="100" Height="30" Click="DisconnectButton_Click" IsEnabled="False" Margin="0,0,8,0" />
                    <Button x:Name="BackfillButton" Content="Backfill from Device" Width="150" Height="30" Click="BackfillButton_Click" IsEnabled="False"
                            ToolTip="Read all transactions stored on the device into the local database (deduped)." />
                    <TextBlock x:Name="LastBackfillText" Text="Last backfill: never" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="Gray" />
                </StackPanel>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <GroupBox Grid.Column="0" Header="Live Punch Feed (last 20)" Margin="0,0,8,0">
                        <ListBox x:Name="FeedList" FontFamily="Consolas" FontSize="12" />
                    </GroupBox>
                    <GroupBox Grid.Column="1" Header="Log">
                        <TextBox x:Name="LogBox" IsReadOnly="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                                 Background="#FF1E1E1E" Foreground="#FFE0E0E0" />
                    </GroupBox>
                </Grid>
            </Grid>

            <!-- Hosted views (Dashboard / Employees / Holidays / Attendance) -->
            <ContentControl x:Name="PageHost" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Window>
